# Phases H0-H7 dependencies -- more are added as later phases need them
# (see PLAN.md section 5, "Tooling introduced per phase"). Versions pinned
# to what was actually installed and verified (every phase's notebook
# executed for real against live data, full test suite green) -- not
# guessed.

# Rasters / vectors / geospatial fundamentals
rasterio==1.5.1
rioxarray==0.23.0
xarray==2026.7.0
geopandas==1.1.4
shapely==2.1.2
rasterstats==0.21.0
pandas==2.3.3
fiona==1.10.1
pyproj==3.8.0
scipy==1.18.1

# Real Microsoft Building Footprints access (H6) -- quadkey-partitioned
# GeoParquet on Azure Blob Storage, needs adlfs directly since
# pystac_client's sign_inplace modifier doesn't sign abfs:// table assets
adlfs==2026.8.0

# Cloud data access (STAC, no account needed)
pystac-client==0.9.0
planetary-computer==1.0.0
stackstac==0.5.1

# Cloud data access (Earth Engine backend -- real API calls, but requires a
# one-time interactive `earthengine authenticate` login not yet done in this
# environment; see notebooks/02_cloud_data_access.ipynb for the real outcome)
earthengine-api==1.7.43
geemap==0.38.5

# Classical ML baseline (H2)
scikit-learn==1.9.1

# SAM 2 label-assist (H2 tests it; real payoff is drone/very-high-res
# imagery in H6 -- see notebooks/03_landcover_baseline.ipynb section 8)
segment-geospatial==1.4.2
torch==2.14.0
torchvision==0.29.0

# Deep semantic segmentation (H3)
segmentation-models-pytorch==0.5.0
torchgeo==0.10.0
albumentations==2.0.8

# Object detection on very-high-res drone imagery (H6)
ultralytics==8.4.150

# EO foundation models -- Prithvi-EO-2.0 via TerraTorch (H7)
terratorch==1.2.13

# Viz
matplotlib==3.11.1

# Notebooks
jupyter==1.1.1
nbconvert==7.17.1
ipykernel==7.3.0

# Tests
pytest==9.1.1
